To run tests:
  - Build library with both client and test code (or two libraries)
  - Import the library(s) into the Unity project
  - In Unity, click Window->Editor Test Runner
  - Click "Run All"

These steps should soon be replaced with Ian's fancy Pythin script.

